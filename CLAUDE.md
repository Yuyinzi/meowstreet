# CLAUDE.md

Claude Code must follow the repository instructions in @AGENTS.md.

`AGENTS.md` is the single source of truth for project intent, operational product rules, architecture constraints, commands, code style, testing, and file structure. Update `AGENTS.md` instead of duplicating shared instructions here.
